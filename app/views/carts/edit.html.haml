%h1 Edit Your Cart

/ -if @errors
/   = pluralize(@errors.count, "error")
/   prohibited this product from being saved:
/   -@product.errors.full_messages.each do |msg|
/     %p=msg

=form_for :carts_product, url: "/carts/#{@cart.id}", method: :patch do |f|

  %table{class: "table table-hover"}
    %tr{}
      %th Product
      %th Price
      %th Quantity
      %th Remove Item
    - @cart_products.each do |prod|
      %tr{class: "small-margin small-padding"}
        %td= prod.title
        %td= prod.price
        %td= f.number_field :quantity, :value => CartsProduct.find_by(cart_id: @cart.id, product_id: prod.id).quantity
        %td "Delete Button"
  %p= f.submit

%p=link_to "Cancel Edit", '/admin/index'

/ %p=link_to "Delete this Product", product_path(@product), method: :delete, data: { confirm: "Confirm Delete?" }
