%h1 Edit #{@user.username}

-if @errors
  = pluralize(@errors.count, "error")
  prohibited this product from being saved:
  -@user.errors.full_messages.each do |msg|
    %p=msg

=form_for :user, url: user_path(@user), method: :patch do |f|

  %p= f.label :email
  %p= f.text_field :email

  %p= f.label :password
  %p= f.number_field :password

  %p= f.submit

%p=link_to "Cancel Edit", user_path(@user)

%p=link_to "Delete this user", user_path(@user), method: :delete, data: { confirm: "Confirm Delete?" }
